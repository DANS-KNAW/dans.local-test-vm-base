<VirtualHost *:80>
  ServerName test
  ProxyTimeout 600

  <Location / >
  ProxyPass http://localhost:{{ backend_port }}/
    ProxyPassReverse http://localhost:{{ backend_port }}/
  </Location>

</VirtualHost>
